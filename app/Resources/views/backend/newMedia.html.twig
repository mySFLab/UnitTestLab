{% extends "::base_admin.html.twig" %}

{% form_theme form "bootstrap_3_layout.html.twig" %}

{% block content %}
    <form action="{{ path('backend_new_media') }}" method="POST" class="form-horizontal">
        {{ form_row(form.title) }}
        {{ form_row(form.url) }}
        {{ form_rest(form) }}
        <input type="submit" value="Valider" class="btn btn-primary"/>
    </form>
{% endblock %}