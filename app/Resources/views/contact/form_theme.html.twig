{% block form_errors -%}
    {% if errors|length > 0 -%}
        <ul>
            {%- for error in errors -%}
                <li><span class="flash-notice">{{ error.message }}</span></li>
            {%- endfor -%}
        </ul>
    {%- endif %}
{%- endblock form_errors %}